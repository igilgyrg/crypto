package main

func main() {
	//logger := logging.GetLogger()
	//logger.Info("create router")
	//
	//cfg := config.GetConfig()
	//
	////background := context.Background()
	//
	//app := candleApp.NewApp(cfg, logger)
	//app.Start()
	//mongoClient, err := mongo.NewClient(background, cfg.MongoHost, cfg.MongoPort, cfg.MongoDatabase, cfg.MongoUsername, cfg.MongoPassword)
	//if err != nil {
	//	panic(err)
	//}
	//
	//candleRepository := mongo2.NewRepository(mongoClient, "sol", logger)
	//candleService := candle.NewService(candleRepository, logger)
	//
	//logger.Info("create handler")
	//userHandler := handler.NewHandler(logger, candleService)
	//userHandler.Register(router)
}
